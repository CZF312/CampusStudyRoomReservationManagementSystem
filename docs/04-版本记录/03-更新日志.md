# 03 — 更新日志

> **工作目录**：`<项目根目录>`  
> 本文件持续记录每次迭代的「计划更新」与「实际完成」。最新条目在最上方。

---

## 2026-06-19 · V3.1 多维度报表导出、全面设置管理与权限隔离

| 编号 | 内容 |
|---|---|
| V31-1 | **多维度报表关联导出**：重构管理员界面“导出报表”为下拉菜单。导出的报表能够与页面下方选择器过滤维度（报表类型如使用率/趋势/高峰/活动/学习时长/信用分、时间范围、自习室范围）完全对应，下载对应维度的动态详情数据。 |
| V31-2 | **系统设置功能全面化**：实现 8 项系统参数（预约提前天数、信用积分限制、违约扣分额度、取消扣分额度、签到积分奖励、每日最大预约数、黑名单时长、单次最大预约时长）管理，通过本地 `system_config.json` 自动加载和写入，持久化配置。 |
| V31-3 | **操作审计日志模块**：设置界面采用双标签页（Tab）设计，集成了操作日志查询。所有管理员的账户启用、禁用、违约撤销、房源删减等重要行为均实时写入 `operation_log` 表并支持界面审计。 |
| V31-4 | **管理员权限安全隔离**：明确只有 `超级管理员` (Super Admin) 拥有系统设置表单的查看与更新权限，普通管理员无权访问该菜单和 API 接口。 |
| V31-5 | **身份展示区快捷菜单**：将退出按钮整合移入左下角身份展示框中，点击时弹出快捷菜单支持“切换账号”或“安全退出”。 |

---

## 2026-06-19 · V3.0 文档大规模更新

| 编号 | 内容 |
|---|---|
| V30-1 | 统一所有文档版本号为 V3.0（移除 V1.1/V1.2 等旧版本标注） |
| V30-2 | 修正 JDK 版本描述（pom.xml 中 `java.version=20`，文档从 JDK 21+ 改为 JDK 20+） |
| V30-3 | 替换所有硬编码工作目录路径（25个文档：`CSRRMupdate1` → `<项目根目录>`） |
| V30-4 | 更新后端模块说明（补充 `UploadStorage.java`、`SqlFragments.java`） |
| V30-5 | 更新数据库实体说明（移除 `temp_leave`，新增 `facility`/`study_room_facility` 表） |
| V30-6 | 重写 `HELP.md`（原 Spring Boot 模板 → 项目相关帮助文档） |
| V30-7 | 重写 `概要设计与详细设计.md` 第5、6、7章（按 V3.0 实际结构） |
| V30-8 | 批量替换第9章英文枚举值为中文（`PENDING`→`待使用` 等） |
| V30-9 | 移除二维码相关接口说明，更新签到方式为学号签到 |
| 验证 | grep 验证旧路径/JDK21/英文枚举引用均已清零 |

---

## 2026-05-27 · V1.3.2 解压后目录导向整理

| 编号 | 内容 |
|---|---|
| V132-1 | 根目录 `00-组长请看这里.txt`（排序靠前）、`解压后目录说明.md` |
| V132-2 | 新增 `docs/00-组长交付/README.md`；README 增加根目录树状图 |
| V132-3 | `delivery-manifest.json` 增加 `leaderEntry` 路径清单 |

---

## 2026-05-27 · V1.3.1 master 合并 + ZIP 下载即用

| 编号 | 内容 |
|---|---|
| V131-1 | V1.3 全量合并入 **master**，GitHub 默认 Download ZIP 即含完整交付 |
| V131-2 | 新增 `组长请看这里.txt`；复现指南改为 ZIP 三步，去掉 git checkout |
| V131-3 | 启动器检测 `database-full.sql` 缺失时提示重新下载 ZIP |

---

## 2026-05-27 · V1.3 组长双击启动 + 脚本修复

| 编号 | 内容 |
|---|---|
| V13-1 | 新增 `组长一键启动.vbs` / `组长一键启动.bat`：无需手动打开 PowerShell，对话框输入 MySQL 密码 |
| V13-2 | 新增 `scripts/leader-run-setup.ps1`、`build-leader-launcher-exe.ps1` |
| V13-3 | 修复 `import-database-local.ps1` 编码导致的 PowerShell 解析失败 |
| V13-4 | `prepare-full-delivery.ps1` 支持中文路径回退查找 |
| V13-5 | 更新 `组长复现指南.md`、`README.md`；交付分支 `v1.3-harrydai4` |

---

## 2026-05-26 · V1.2.1 学号签到 + 数据库交付强化

| 编号 | 内容 |
|---|---|
| V121-1 | 管理端签到改为输入**学号**；学生端取消二维码/token |
| V121-2 | 移除实时摄像头/拍照扫码（jsQR、qrcode 依赖） |
| V121-3 | 后端 `scanCheckin` 支持 `studentNo`；签到方式 `STUDENT_NO` |
| V121-4 | 新增 `课设数据库交付清单.md`；重新导出 schema/data/full SQL |
| 验证 | `mvnw test`、`build-frontend.ps1` 通过 |

---

## 2026-05-26 · V1.2 课设交付：数据库 SQL + 共用/签到增强

| 编号 | 内容 |
|---|---|
| V12-1 | 新增 `docs/06-部署配置/schema.sql`、`data.sql`、`database-full.sql`（mysqldump 导出，课设/组长要求） |
| V12-2 | 新增 `scripts/export-database-for-git.ps1`、`import-database-local.ps1` |
| V12-3 | 新增 `docs/06-部署配置/数据库文件说明.md`；更新 GitHub 提交清单、README |
| V12-4 | 多人共用：`07`/`08` 指南、共用脚本、Tailscale/Cloudflare 隧道脚本 |
| V12-5 | 签到：±15 分钟时间窗、无效签到撤销、签退 slot 冲突修复、拍照扫码（jsQR+qrcode） |
| V12-6 | 管理端 i18n、上传路径加固、连接池与 `application-shared` 配置 |
| 验证 | `mvnw test`、`build-frontend.ps1` 通过 |

---

## 2026-05-26 · 最终版验收 + GitHub 全流程文档

| 编号 | 内容 |
|---|---|
| F1 | 核对 `问题记录.md` 1～10 条，全部标记 ✅ |
| F2 | 新增 `docs/03-开发维护/最终验收对照.md` |
| F3 | 新增 `docs/01-使用指南/06-GitHub傻瓜式提交全流程.md` |
| F4 | 学习统计 `stats-tabs` 改为 4 列（含年报） |
| 验证 | `mvnw test`、`build-frontend.ps1` 通过 |

---

## 2026-05-26 · 启动流程文档（PowerShell 可复制）

| 编号 | 内容 |
|---|---|
| S1 | 新增 `docs/01-使用指南/05-启动流程与PowerShell命令.md` |
| S2 | 更新根 `README.md`、`docs/README.md`、01/03 指南链接 |
| S4 | 重写 `04-GitHub提交清单与说明.md` 为傻瓜式逐步指南（含 PAT、自检表） |

---

## 2026-05-25 · `docs/` 子目录中文命名与序号

| 编号 | 原目录 | 新目录 |
|---|---|---|
| D1 | `guides/` | `01-使用指南/` |
| D2 | `architecture/` | `02-架构说明/` |
| D3 | `development/` | `03-开发维护/` |
| D4 | `changelog/` | `04-版本记录/` |
| D5 | `spec/` | `05-需求与设计/` |
| D6 | `deploy/` | `06-部署配置/` |
| D7 | `prototype/` | `07-原型资源/` |

全项目内文档链接、根 `README.md`、`prototype-ui.css` 注释已同步更新。

---

## 2026-05-26 · 提交前自检清单 + GitHub 指南 + 校验补强

| 编号 | 内容 |
|---|---|
| G1 | 新增 `docs/01-使用指南/03-提交前全面自检清单.md`（文档+原型+§9+V1.1+API） |
| G2 | 新增 `docs/01-使用指南/04-GitHub提交清单与说明.md` |
| G3 | 注册：手机/邮箱/密码强度/材料必传校验 |
| G4 | 预约：信用≤0 前端提示与禁用预约按钮 |
| 验证 | `mvnw test`、`npm run build` 通过 |

---

### 计划更新

| 编号 | 内容 |
|---|---|
| U3 | 对照原型+设计文档 §8–§9，完成 M00–M21 UI/交互 |
| U4 | 重写 UI 对齐总工作文档 + 进度摘要 |
| U5 | 后端 dashboard/统计/注册上传 API 扩展 |

### 实际完成

| 编号 | 状态 | 说明 |
|---|---|---|
| U3 | ✅ | 学生端：登录/首页/预约/签到弹窗/我的/统计/反馈 modal 化 |
| U3 | ✅ | 管理端：四 KPI 仪表盘、内嵌自习室表单、座位网格编辑、统计三 Tab |
| U4 | ✅ | `docs/03-开发维护/UI对齐总工作文档.md` |
| U5 | ✅ | dashboard 扩展字段、`/auth/register/upload`、`/admin/admins` |
| 验证 | ✅ | `mvnw test` 通过；`npm run build` 通过 |

---

### 计划更新


| 编号  | 内容                         |
| --- | -------------------------- |
| R1  | 重分类 M1～M8：必做 / 可选 / 助手已完成  |
| R2  | 前端 build、`mvn test`（H2）自动化 |
| R3  | 脚本、部署模板、本地配置示例             |


### 实际完成


| 编号  | 状态  | 说明                                                                                                         |
| --- | --- | ---------------------------------------------------------------------------------------------------------- |
| R1  | ✅   | 新增 [01-必做与分工说明.md](../01-使用指南/01-必做与分工说明.md)                                                                |
| R2  | ✅   | `npm run build` 成功；`mvnw test` 2 项通过（含定时违约测试）                                                              |
| R3  | ✅   | `scripts/check-env.ps1`、`scripts/build-frontend.ps1`、`docs/06-部署配置/`、`application-local.properties.example` |


### 代码修正

- `DatabaseInitializer`：H2 兼容 + 修复连接泄漏
- `AppService.scheduledProcessNoShow`：Java 侧判断超时，便于测试与跨库
- `src/test/resources/application-test.properties`：H2 + 足够长的 JWT secret

---

## 需人工操作 / 分工说明（已重分类，2026-05-25）

> **详细对照** → [01-必做与分工说明.md](../01-使用指南/01-必做与分工说明.md)

### A. 必做 · 仅你能做


| 编号  | 事项                | 说明                                                               |
| --- | ----------------- | ---------------------------------------------------------------- |
| M1  | **安装并启动 MySQL 8** | 助手无法替你安装/启动本机数据库服务；可选 `docs/06-部署配置/docker-compose.yml`（需 Docker） |
| M2  | **配置数据库密码**       | root 有密码时设置 `DB_PASSWORD` 或 `application-local.properties`       |
| M8  | **课程报告 Word**     | `.docx` 需在 Word 中编辑，助手只提供 Markdown 变更说明                          |


### B. 必做 · 助手已完成（你只需启动验收）


| 编号  | 事项          | 说明                                                                      |
| --- | ----------- | ----------------------------------------------------------------------- |
| M3  | Java 20+ 编译 | ✅ 本机已 `compile` / `test` 通过                                             |
| M4  | 前端构建        | ✅ 已 `npm install` + `npm run build`；改前端后运行 `scripts/build-frontend.ps1` |
| M6  | 验证定时任务      | ✅ `AppServiceScheduledTest` + `mvnw test`，无需手插 SQL                      |


### C. 必做 · 需你执行启动命令


| 事项    | 命令                                                                       |
| ----- | ------------------------------------------------------------------------ |
| 启动后端  | `.\mvnw.cmd spring-boot:run`                                             |
| 浏览器验收 | [http://localhost:8080（前端已打包，可不启](http://localhost:8080（前端已打包，可不启) 5173） |


### D. 可选 · 加分 / 答辩后


| 编号  | 事项       | 说明                                   |
| --- | -------- | ------------------------------------ |
| M5  | 手机真机扫码   | 同 WiFi 访问电脑 IP；可改用手动粘贴 token         |
| M7  | Nginx 部署 | 已提供 `docs/06-部署配置/nginx.conf.example` |


---

## 2026-05-25 · 项目目录整理与文档体系

### 计划更新


| 编号  | 内容                                   |
| --- | ------------------------------------ |
| O1  | 根目录只保留代码与 README，文档迁入 `docs/`        |
| O2  | 新增架构说明、目录规范、傻瓜式全局讲解                  |
| O3  | 更新 README、开发原则、`.gitignore`（uploads） |


### 实际完成


| 编号  | 状态  | 说明                                                                   |
| --- | --- | -------------------------------------------------------------------- |
| O1  | ✅   | 创建 `docs/{guides,architecture,development,changelog,spec,prototype}` |
| O2  | ✅   | 新增 `docs/01-使用指南/00-傻瓜式全局项目讲解.md` 等                                   |
| O3  | ✅   | 根目录文档已迁移；`docs/README.md` 为文档索引                                      |


### 迁移对照


| 原路径（根目录）       | 新路径                           |
| -------------- | ----------------------------- |
| `00-开发原则.md`   | `docs/03-开发维护/00-开发原则.md` |
| `01~03-*.md`   | `docs/04-版本记录/`             |
| `问题记录.md`      | `docs/03-开发维护/问题记录.md`    |
| `概要设计与详细设计.md` | `docs/05-需求与设计/`                  |
| `原型设计.html`    | `docs/07-原型资源/`             |
| `*.docx`       | `docs/05-需求与设计/`                  |


---

## 2026-05-25 · V1.1 基线补齐（第一轮）

### 计划更新


| 编号  | 内容                                  | 优先级 |
| --- | ----------------------------------- | --- |
| U1  | 定时任务：未签到违约、自动签退、黑名单解除、暂离超时          | P0  |
| U2  | 暂离 API + 前端按钮                       | P0  |
| U3  | operation_log 表 + 管理操作写日志 + 管理设置页展示 | P0  |
| U4  | 学生资料 PUT /student/profile           | P0  |
| U5  | 文件上传 POST /api/upload + 自习室分布图      | P0  |
| U6  | 数据库视图 v_room_daily_usage            | P1  |
| U7  | 文档体系 00~03 + README                 | P0  |


### 实际完成


| 编号  | 状态  | 说明                                                                |
| --- | --- | ----------------------------------------------------------------- |
| U1  | ✅   | 新增 `ScheduledTaskService`，每分钟调用 `AppService` 四类维护方法               |
| U2  | ✅   | `temp_leave` 表；`POST .../temp-leave`、`.../temp-return`；签到页暂离/返回按钮 |
| U3  | ✅   | `operation_log` 表；自习室增删改、用户审核写日志；`GET /admin/operation-logs`      |
| U4  | ✅   | `updateProfile()` 写入 student_profile；设置页表单保存                      |
| U5  | ✅   | `UploadController`；`WebConfig` 映射 `/uploads/`**；管理端选图上传           |
| U6  | ✅   | `DatabaseInitializer.createViews()` 创建 `v_room_daily_usage`       |
| U7  | ✅   | 创建 `00-开发原则.md`、`01-原始内容总结.md`、`02-更新版说明.md`、本文件、`README.md`      |


### 修改文件清单

**后端**

- `CampusStudyRoomReservationManagementSystemApplication.java` — `@EnableScheduling`
- `service/AppService.java` — 暂离、定时任务、资料更新、操作日志
- `service/ScheduledTaskService.java` — 新建
- `service/DatabaseInitializer.java` — temp_leave、operation_log、视图
- `controller/AppController.java` — 新接口、auditUser 签名
- `controller/UploadController.java` — 新建
- `config/WebConfig.java` — 新建
- `config/SecurityConfig.java` — 放行 `/uploads/`**
- `application.properties` — 上传目录与大小限制

**前端**

- `frontend/src/App.vue` — 暂离、设置、上传、操作日志
- `frontend/src/styles.css` — 暂离提示、上传预览样式

**文档**（已整理至 `docs/`）

- `docs/01-使用指南/00-傻瓜式全局项目讲解.md`
- `docs/02-架构说明/`、`docs/03-开发维护/`、`docs/04-版本记录/`
- 根目录 `README.md`

---

## 下一版本（V1.2）建议待办

- 后端分层重构（Entity / Mapper / Service）
- MySQL 触发器写入 operation_log（报告 SQL 附录）
- jsQR 扫码 fallback
- 修改密码接口
- 更多集成测试用例（签退、暂离超时等）

